# OneTrust Vercel Login Demo

One repository, three Vercel projects:
- site-a
- site-b
- jwt-api

Credentials are not stored in the front-end. The JWT API validates values stored in Vercel environment variables.
